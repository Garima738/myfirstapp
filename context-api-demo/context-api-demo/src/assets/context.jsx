import { createContext } from "react";

//Empty Context
export const GlobalContext=createContext(); //as per industry global is used inplace of theme.



// export const UserContext=createContext();

// //Default context
// export const LangContext=createContext("en");